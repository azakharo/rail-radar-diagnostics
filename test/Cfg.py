#!/usr/bin/python
# -*- coding: utf-8 -*-

# Add the source root to the import paths
from os.path import join, dirname
import sys

SRC_ROOT = join(dirname(__file__), "..", 'src')
sys.path.append(SRC_ROOT)
